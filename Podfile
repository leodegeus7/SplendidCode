# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!
target 'SplendidCode' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  
  pod 'SwiftLint'
  # Pods for SplendidCode

  target 'SplendidCodeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SplendidCodeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'Networking' do
  pod 'SwiftLint'
end
